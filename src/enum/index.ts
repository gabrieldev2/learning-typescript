enum Cores {
  VERMELHO, // 0
  AZUL, // 1
  VERDE, // 2
}

enum Cores {
  ROSA = 5, // 5
  ROXO, // 6
}

// console.log(Cores);
console.log(Cores.VERMELHO);
console.log(Cores[0]);
console.log(Cores);
